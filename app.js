window.addEventListener('load', () => {
    const preload = document.querySelector('.preload');
    preload.classList.add('preload-finish');
})

window.addEventListener('load', () => {
    const welcome = document.querySelector('.welcome');
    welcome.classList.add('welcome-after');
})
